/* eslint-disable @typescript-eslint/no-magic-numbers -- time values are intuitive here */
// defines constants for time periods in milliseconds as numbers
const SECOND = 1000;
const MINUTE = 60 * SECOND;
const HOUR = 60 * MINUTE;
const DAY = 24 * HOUR;

export { DAY, HOUR, MINUTE, SECOND };
