import { test } from "../../fixtures/index.js";

test.fixme(
  "@e2e multi-office user can change office and return to case list",
  async () => {},
);

test.fixme(
  "@e2e after changing offices, case list only shows cases for selected office",
  async () => { },
);