import { test } from "../../fixtures/index.js";

test.fixme(
  "@e2e means incomplete blocks evidence and declaration tasks",
  async () => {},
);

test.fixme(
  "@e2e evidence incomplete blocks declaration tasks",
  async () => { },
);

test.fixme(
  "@e2e declaration incomplete blocks submission",
  async () => { },
);